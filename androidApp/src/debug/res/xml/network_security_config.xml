<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug builds only — this file has no counterpart in src/main, so release builds keep the
  platform default of HTTPS-only and cannot be pointed at a cleartext host by accident.

  Cleartext is permitted for the two loopback aliases a dev server is reached on and nothing
  else: 10.0.2.2 is the host machine as seen from the emulator, localhost is the device itself
  (adb reverse). Testing against a server on the LAN means adding that address here, knowingly.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
